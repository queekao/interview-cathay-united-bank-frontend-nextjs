import { styled } from '@mui/material/styles'
export const Main = styled('main')(() => ({
  borderBottomLeftRadius: 0,
  borderBottomRightRadius: 0,
  height: '100%',
  margin: '12rem 28rem 8.4rem 28rem'
}))
